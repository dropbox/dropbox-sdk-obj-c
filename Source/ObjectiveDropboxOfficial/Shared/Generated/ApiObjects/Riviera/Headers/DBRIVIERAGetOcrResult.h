///
/// Copyright (c) 2016 Dropbox, Inc. All rights reserved.
///
/// Auto-generated by Stone, do not modify.
///

#import <Foundation/Foundation.h>

#import "DBSerializableProtocol.h"

@class DBRIVIERAGetOcrResult;

NS_ASSUME_NONNULL_BEGIN

#pragma mark - API Object

///
/// The `GetOcrResult` struct.
///
/// This class implements the `DBSerializable` protocol (serialize and
/// deserialize instance methods), which is required for all Obj-C SDK API route
/// objects.
///
@interface DBRIVIERAGetOcrResult : NSObject <DBSerializable, NSCopying>

#pragma mark - Instance fields

/// The plain-text content extracted from the file via OCR. Words within a line
/// are separated by a single space, lines are newline-separated in reading
/// order, and for multi-page PDFs pages are separated by a blank line in page
/// order. May be empty when no text is detected in the source.
@property (nonatomic, readonly, copy) NSString *text;

/// The same content as hOCR: HTML that carries the position of every recognized
/// word. Each page is a `<section>` holding `<p class="line">` elements with
/// one `<span>` per word, and each element carries `data-x`, `data-y`,
/// `data-width`, and `data-height` attributes in pixels relative to the upright
/// page (whose dimensions are on the `<section>`). Use this when you need word
/// coordinates -- to highlight matches over a page image, for example; use
/// `text` when you just need the words.
@property (nonatomic, readonly, copy) NSString *hocr;

#pragma mark - Constructors

///
/// Full constructor for the struct (exposes all instance variables).
///
/// @param text The plain-text content extracted from the file via OCR. Words
/// within a line are separated by a single space, lines are newline-separated
/// in reading order, and for multi-page PDFs pages are separated by a blank
/// line in page order. May be empty when no text is detected in the source.
/// @param hocr The same content as hOCR: HTML that carries the position of
/// every recognized word. Each page is a `<section>` holding `<p class="line">`
/// elements with one `<span>` per word, and each element carries `data-x`,
/// `data-y`, `data-width`, and `data-height` attributes in pixels relative to
/// the upright page (whose dimensions are on the `<section>`). Use this when
/// you need word coordinates -- to highlight matches over a page image, for
/// example; use `text` when you just need the words.
///
/// @return An initialized instance.
///
- (instancetype)initWithText:(nullable NSString *)text hocr:(nullable NSString *)hocr;

///
/// Convenience constructor (exposes only non-nullable instance variables with
/// no default value).
///
///
/// @return An initialized instance.
///
- (instancetype)initDefault;

- (instancetype)init NS_UNAVAILABLE;

@end

#pragma mark - Serializer Object

///
/// The serialization class for the `GetOcrResult` struct.
///
@interface DBRIVIERAGetOcrResultSerializer : NSObject

///
/// Serializes `DBRIVIERAGetOcrResult` instances.
///
/// @param instance An instance of the `DBRIVIERAGetOcrResult` API object.
///
/// @return A json-compatible dictionary representation of the
/// `DBRIVIERAGetOcrResult` API object.
///
+ (nullable NSDictionary<NSString *, id> *)serialize:(DBRIVIERAGetOcrResult *)instance;

///
/// Deserializes `DBRIVIERAGetOcrResult` instances.
///
/// @param dict A json-compatible dictionary representation of the
/// `DBRIVIERAGetOcrResult` API object.
///
/// @return An instantiation of the `DBRIVIERAGetOcrResult` object.
///
+ (DBRIVIERAGetOcrResult *)deserialize:(NSDictionary<NSString *, id> *)dict;

@end

NS_ASSUME_NONNULL_END
